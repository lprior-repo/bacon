version: '2'

run:
  timeout: 5m

linters:
  enable:
    - govet
    - staticcheck 
    - unused
    - errcheck
    - ineffassign
    - gosec
    - goconst
    - revive

issues:
  max-issues-per-linter: 0
  max-same-issues: 0